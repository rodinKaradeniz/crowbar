{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": ["/server/**"]
  },
  "deploy": {
    "startCommand": "python -m app.jobs.reservation_reminders",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
