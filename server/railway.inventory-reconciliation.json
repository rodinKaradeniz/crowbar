{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": ["/server/**"]
  },
  "deploy": {
    "startCommand": "python -m app.jobs.inventory_reconciliation",
    "cronSchedule": "30 4 * * *",
    "restartPolicyType": "NEVER"
  }
}
