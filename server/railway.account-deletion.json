{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": ["/server/**"]
  },
  "deploy": {
    "startCommand": "python -m app.jobs.account_deletion",
    "cronSchedule": "30 3 * * *",
    "restartPolicyType": "NEVER"
  }
}
