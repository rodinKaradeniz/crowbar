{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": ["/server/**"]
  },
  "deploy": {
    "startCommand": "python -m app.jobs.customer_retention",
    "cronSchedule": "0 4 * * *",
    "restartPolicyType": "NEVER"
  }
}
