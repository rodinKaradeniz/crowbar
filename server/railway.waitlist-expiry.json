{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": ["/server/**"]
  },
  "deploy": {
    "startCommand": "python -m app.jobs.reservation_waitlist_expiry",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
